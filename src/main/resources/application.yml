spring:
  profiles:
    active: ${SPRING_PROFILE:local}  # perfil por defecto

  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}  # crea o actualiza las tablas automáticamente
    show-sql: ${JPA_SHOW_SQL:true}
    properties:
      hibernate:
        '[format_sql]': ${JPA_FORMAT_SQL:true}

# Configuración de JWT (JSON Web Token)
jwt:
  # Clave secreta para firmar y verificar tokens JWT
  # IMPORTANTE: Cambiar en producción y mantener en variables de entorno
  secret: ${JWT_SECRET:mySecretKeyForPolloEmpoderadoBackend2024}
  # Tiempo de expiración del token en milisegundos (24 horas = 86400000 ms)
  expiration: ${JWT_EXPIRATION:86400000}

# Configuración de CORS (Cross-Origin Resource Sharing)
cors:
  # Orígenes permitidos para realizar peticiones al backend
  # En producción, especificar solo los dominios autorizados
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:4200}
  # Métodos HTTP permitidos
  allowed-methods: ${CORS_ALLOWED_METHODS:GET,POST,PUT,DELETE,OPTIONS}
  # Headers permitidos en las peticiones
  allowed-headers: ${CORS_ALLOWED_HEADERS:*}
  # Permitir credenciales (cookies, authorization headers)
  allow-credentials: ${CORS_ALLOW_CREDENTIALS:true}

# Configuración de MercadoPago
mercadopago:
  access-token: ${MERCADOPAGO_ACCESS_TOKEN:TEST-6645123649247854-121102-3162dad69521755e2a4dd7dc911d5c89-608720469}
  public-key: ${MERCADOPAGO_PUBLIC_KEY:TEST-85e01ca4-16a5-4f4b-9335-90dda2c3de08}

# URL base del frontend (para redirecciones de pago)
app:
  base-url: ${APP_BASE_URL:http://localhost:4200}
