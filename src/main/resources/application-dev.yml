spring:
  datasource:
    url: jdbc:mysql://dev-server:3306/mi_app_db?useSSL=false&serverTimezone=America/Lima
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
